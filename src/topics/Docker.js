import React from 'react';
import { Typography } from '@mui/material';

const Docker = () => (
    <section id="docker" style={{ paddingTop: '5%' }}>
      <Typography variant="h6">Docker Section</Typography>
      <p>This is the docker section</p>
    </section>
  );


export default Docker;