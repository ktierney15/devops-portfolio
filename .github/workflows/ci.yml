# This is a basic workflow to help you get started with Actions

name: CI
on:
  push: 
    branches: ["main"]
  pull_request:
    branches: [ "main", "develop" ]


  workflow_dispatch:

jobs:
  Build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Set up NodeJS environment
        uses: actions/setup-node@v2
        with:
          node-version: 18

      - name: Install package dependencies
        run: npm install

#       - name: Check linting and formatting
#         run: |
#           npm install -g eslint
#           eslint **/src/*.js


# # TODO: create unit testing
#       # - name: Execute test cases
#       #   run: npm run test
      
#       - name: Create test build
#         run: npm run build

# TODO: once terraform is added - run a terraform init and plan



  Version-and-Publish:
    runs-on: ubuntu-latest
    if: github.event_name != 'pull_request'
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v2
        with:
          fetch-depth: 0
          ref: ${{ github.sha }}

      - name: Sign in to Git
        run: |
          git config user.name "${{ github.actor }}"
          git config user.email "${{ github.actor }}@users.noreply.github.com"

      - name: Get Latest Tag
        id: get-latest-tag
        run: echo "::set-output name=TAG::$(git describe --tags --abbrev=0)"

      - name: Get commit message
        run: |
          MERGE_COMMIT_SHA=$(git log --merges --pretty=format:'%H' -n 1)
          echo "commit_message=$(git log --format=%s -n 1 $MERGE_COMMIT_SHA)" >> $GITHUB_ENV

      - name: Set increment type variable
        run: |
          if [[ "${{ env.commit_message }}" == *"PATCH"* ]]; then
            echo "version_increment_type=patch" >> $GITHUB_ENV 
          elif [[ "${{ env.commit_message }}" == *"MAJOR"* ]]; then
            echo "version_increment_type=major" >> $GITHUB_ENV 
          else
            echo "version_increment_type=minor" >> $GITHUB_ENV
          fi
      
      - name: Semantic Versioning
        run: echo "updated_tag=$(python3 .github/scripts/sem-ver.py ${{ env.version_increment_type }} ${{ steps.get-latest-tag.outputs.TAG }})" >> $GITHUB_ENV

      # - name: Tag new version
      #   run: |
      #     git remote set-url origin https://ktierney15:${{ secrets.PASSWORD }}@github.com/${{ github.repository_owner }}/${{ github.event.repository.name }}.git
      #     git tag ${{ env.updated_tag }}
      #     git push origin --tags

      - uses: rickstaa/action-create-tag@v1
        with:
          tag: ${{ env.updated_tag }}
          tag_exists_error: false
          message: "Version ${{ env.updated_tag }}"
          
  # https://ktierney15:${{ secrets.PASSWORD }}@github.com/${{ github.repository_owner }}/${{ github.event.repository.name }} --tags
        # env:
        #   GITHUB_TOKEN: ${{ secrets.TOKEN }}
          
############################################

      # - name: Login to DockerHub
      #   uses: docker/login-action@v1
      #   with:
      #     username: ${{ secrets.DOCKER_USERNAME }}
      #     password: ${{ secrets.DOCKERHUB_TOKEN }}

      # - name: Build and push
      #   uses: docker/build-push-action@v2
      #   with:
      #     context: ./
      #     file: ./Dockerfile
      #     push: true
      #     tags: ${{ secrets.DOCKER_USERNAME }}/devops_portfolio:${{ env. }}






